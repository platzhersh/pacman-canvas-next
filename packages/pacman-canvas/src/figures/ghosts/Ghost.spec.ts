// import { inkySvgSrc } from "../../assets/img";
// import { Ghost } from "./Ghost";

// class GhostImpl extends Ghost {
//   protected getChaseModeTarget = (
//     _game: any,
//     _pacman: any
//   ): [targetX: number, targetY: number] => {
//     return [0, 0];
//   };
//   public checkDirectionChange = (game: any) => {};
//   constructor(game: any) {
//     super(game, "test", 0, 0, inkySvgSrc, 0, 0);
//   }
// }

describe("Ghost", () => {
  test("should be defined", () => {
    // TODO;
  });
  //   let game: any;
  //   let ghost: GhostImpl;
  //   beforeEach(() => {
  //     game = {};
  //     ghost = new GhostImpl(game);
  //     ghost.undazzle = jest.fn();
  //   });
  //   describe("reset", () => {
  //     beforeEach(() => {
  //       ghost.reset(game);
  //     });
  //     test("should set dead to false", () => {
  //       expect(ghost.isDead()).toEqual(false);
  //     });
  //     test("should set posX to startPosX", () => {
  //       expect(ghost.getGridPosX()).toEqual(ghost.getStartPosX);
  //     });
  //     test("should set posY to startPosY", () => {
  //       expect(ghost.getStartPosY).toEqual(ghost.getStartPosY());
  //     });
  //     test("should set ghostHouse to true", () => {
  //       expect(ghost.isInGhostHouse()).toEqual(true);
  //     });
  //     test("should set dirX to 0", () => {
  //       expect(ghost.getPosX()).toEqual(0);
  //     });
  //     test("should set dirY to 0", () => {
  //       expect(ghost.getPosY()).toEqual(0);
  //     });
  //     xtest("should call directionWatcher.set with null", () => {
  //       expect(ghost.getDirectionWatcherValue()).toEqual(null);
  //     });
  //     xtest("should call undazzle with game", () => {
  //       expect(ghost.undazzle).toHaveBeenCalledWith(game);
  //     });
  //   });
});
